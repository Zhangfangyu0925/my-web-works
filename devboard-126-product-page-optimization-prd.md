# DEVBOARD-126 Devboard/Product 整体二期优化

## 1. 背景

当前 DevBoard Product 页面已具备产品列表、产品主页、产品管理、产品文档展示等基础能力，但在实际使用中仍存在以下问题：

1. 产品主页缺少直达维护入口，用户从产品主页进入维护流程时无法直接定位到当前产品，操作链路偏长。
2. 文档展示范围缺少产品级 `nav.yaml` 导航配置，仓库结构不规范时容易在前端暴露过多无关文件。
3. 文档访问数据缺少汇总，产品负责人无法快速判断文档使用情况。
4. 产品文档在 GitHub 仓库、图片渲染、表格换行、文件展示控制等方面存在兼容性和可读性问题。
5. 产品文档当前不支持在正文内直接嵌入仓库内 HTML 原型。
6. 产品文档中的仓库相对路径链接会跳转到 GitLab，而不是留在 DevBoard 内继续阅读。
7. 产品文档缺少文档内章节导航，长文档只能靠滚动查找标题，不便于定位和分享。

## 2. 目标

1. 在产品主页提供直达当前产品维护态的入口。
2. 为产品文档增加产品级 `nav.yaml` 导航配置。
3. 为产品文档访问行为补充可统计的埋点基础数据。
4. 解决产品文档的兼容性、可读性渲染问题。
5. 支持产品文档在正文内嵌入仓库内 HTML 原型。
6. 支持产品文档通过相对路径在 DevBoard 内跳转到仓库内其他文档。
7. 优化产品主页的文档浏览交互体验。
8. 在现有 Product 文档标题锚点能力基础上，补充 hover 悬浮目录与横杠章节提示。

## 3. 范围

### 本期范围

1. 产品主页增加“产品编辑”入口。
2. 现有 `/product/manage` 支持根据页面参数或页面状态直接打开对应产品编辑态。
3. 产品管理页增加产品级 `nav.yaml` 导航配置项。
4. 产品文档访问相关行为补充埋点，覆盖 PV / UV 所需基础数据。
5. 解决 GitHub 文档仓库媒体文件渲染问题
6. 文档库渲染支持表格内 `<br>` 换行
7. 产品文档支持在正文内 iframe 嵌入仓库内 HTML 原型文件
8. 产品文档支持通过相对路径在 DevBoard 内跳转到仓库内其他文档
9. 产品文档支持标题锚点与 hover 悬浮目录

### 本期不包含

1. 不新增独立的产品维护页或产线维护页。
2. 不重构现有产品/产线编辑表单结构。
3. 本期不在前端展示 PV / UV 数据或汇总看板。

## 4. 详细需求

## 页面逻辑优化迭代：

### 4.1 产品主页维护入口直达

#### 需求说明

1. 产品主页增加“产品编辑”入口。
2. 点击“产品编辑”后，仍进入现有 `/product/manage`，不新增独立维护页。
3. `/product/manage` 需支持根据页面参数或页面状态直接定位到目标对象并打开编辑态。
4. 第一阶段至少支持从产品主页进入后：
   - 自动切换到“产品管理” tab
   - 自动定位当前产品
   - 自动打开当前产品编辑弹窗

#### 方案边界

1. 本期不要求重做 Product 管理页的双 tab 结构。
2. 本期不要求拆分独立编辑页，仅要求现有维护页支持轻量直达。
3. 当目标产品不存在、无权限或数据加载失败时，应回退到对应 tab 的默认列表态，而不是进入空白页。

#### 验收标准

1. 从产品主页点击“产品编辑”后，可进入 `/product/manage` 并直接打开当前产品编辑弹窗。
2. 进入 `/product/manage` 后默认落在“产品管理” tab，不需要用户二次切换。
3. 异常情况下页面可正常回退到可操作列表态。

### 4.2 `nav.yaml` 导航配置

#### 需求说明

1. Product 文档支持通过仓库内 `nav.yaml` 定义文档展示范围与左侧导航结构。
2. `nav.yaml` 用于控制 DevBoard 内的默认文档导航、展示顺序、分组结构、展示标题和默认首页。
3. `nav.yaml` 只影响 DevBoard 内的文档树展示与站内阅读入口，不限制仓库实际内容。
4. 第一阶段导航配置支持的核心字段为：
   - `home`
   - `nav`
   - `title`
   - `path`
   - `children`
5. 未在 `nav.yaml` 中声明的文件，第一阶段不进入默认左侧导航展示。
6. 配置项按产品维度生效，并随产品文档配置一并维护。

#### 配置要求

1. 系统在当前 Product 文档 `doc_root` 下查找 `nav.yaml`；若未找到，可兼容查找 `nav.yml`。
2. `home` 用于指定 Product 文档默认首页，其优先级高于现有 `README` / `index` 入口规则。
3. 若未配置 `home`，系统取 `README.md` 文档作为默认入口。
4. 若 `README.md` 不存在，系统回退到现有 `index_files` / `README` 入口逻辑。
5. `path` 只能指向当前 Product 文档根目录内的实际文件，不允许越出文档根目录。
6. 若产品未配置 `nav.yaml`，系统继续使用现有仓库文件树与默认入口策略。

#### 降级规则

1. `nav.yaml` 不存在时，不报错，按现有文件树逻辑展示。
2. `nav.yaml` 解析失败、路径非法、目标文件不存在时，不阻断页面，统一回退到现有文件树逻辑。
3. 降级后，正文预览、相对路径站内跳转、HTML iframe 预览等现有能力保持可用。

#### 验收标准

1. 仓库存在合法 `nav.yaml` 时，Product 文档左侧导航按配置的标题、顺序和分组展示。
2. 配置 `home` 后，进入 Docs 页默认打开 `home` 指向的文档。
3. 未配置 `home` 时，进入 Docs 页默认打开 `README.md` 文档。
4. 未在 `nav.yaml` 中声明的文件，不出现在默认左侧导航中。
5. 点击导航项后，正文正确打开目标文件，当前项高亮正确。
6. `nav.yaml` 不存在或非法时，页面不白屏、不阻断阅读，并回退到现有文件树逻辑。

### 4.3 文档访问指标

#### 需求说明

1. 对产品文档文件页增加访问行为埋点能力。
2. 埋点范围至少包含：
   - 文档文件页访问
   - 文档切换
   - 文件树点击
   - 相对路径站内跳转
   - iframe 原型加载
3. 埋点数据需覆盖 PV / UV 所需基础字段，满足后续统计分析使用。
4. 本期不在产品主页或文档文件页前端展示 PV / UV 数据。

#### 验收标准

1. 产品文档关键访问行为均有可用埋点。
2. PV / UV 所需基础数据可在埋点侧采集到。
3. 本期前端页面不展示 PV / UV 数值或相关统计模块。

### 4.4 Product 文档支持 iframe 嵌入仓库内 HTML 原型

#### 需求说明

1. Product 文档支持在正文内嵌入当前产品文档仓库中的 HTML 原型文件。
2. 第一阶段仅支持通过仓库内相对路径引用 HTML 文件。
3. iframe 内引用的 CSS、JS、图片等静态资源，应按仓库相对路径正常加载。
4. iframe 内跳转到同仓库其他 HTML 页面时，应继续在 iframe 内打开。
5. 文件不存在、路径越界或加载失败时，页面应提供明确失败提示。

#### 方案边界

1. 本期不支持嵌入外部 URL。
2. 本期不支持跨仓库、跨产品读取 HTML 文件。
3. 仅允许加载当前产品文档根目录内的 `.html` / `.htm` 文件。
4. 相对路径不能越过当前产品文档根目录。

#### 验收标准

1. 文档中引用仓库内 HTML 文件后，可直接以内嵌 iframe 形式展示。
2. 原型引用的仓库内静态资源可正常加载。
3. 原型内跳转同仓库其他 HTML 页面时，页面可正常打开。
4. 文件不存在、越界或加载失败时，页面有明确错误提示。**“当前原型无法内嵌预览，请在仓库中打开原文件查看”** 并提供“打开原文件”按钮

### 4.5 Product 文档支持通过相对路径跳转到仓库内其他文档

#### 需求说明

1. 点击产品文档中的仓库内相对路径链接时，系统应优先解析为当前产品文档树内文件。
2. 目标文件属于已支持阅读类型时，应在当前 Product 页内切换，不跳 GitLab。
3. 支持 `./`、`../` 等常见相对路径写法。
4. 支持 `./faq.md#token` 这类跨文件锚点跳转。
5. 同文件锚点应在当前页面内直接定位。
6. 外部绝对链接保持原有跳转行为。

#### 方案边界

1. 本期不改写外部 URL 跳转行为。
2. 本期不支持跨产品、跨仓库站内跳转。
3. 仅当前产品文档根目录内的相对路径允许走站内跳转。
4. 超出文档根目录或目标文件类型不支持时，按现有降级逻辑处理。

#### 验收标准

1. 点击 `./manual/install.md`、`../faq.md` 这类链接时，页面留在 DevBoard 内并切换到目标文档。
2. 点击 `./manual/install.md#quick-start` 时，可进入目标文档并定位到对应位置。
3. 跳转后文件树高亮同步更新，当前页面阅读链路不被打断。
4. 超出文档根目录、文件不存在或类型不支持时，不误走站内跳转。

## 文档渲染类：

### 4.6 GitHub 仓库媒体文件渲染问题

#### 需求说明

1. GitHub 仓库中的图片资源可在产品主页文档区正常渲染。
2. 兼容常见相对路径引用方式。
3. GitLab 仓库现有渲染能力不退化。
4. 渲染失败时提供明确降级提示。“渲染失败，请刷新重试；若仍失败，请到仓库查看” 并提供“打开原文件”按钮

#### 验收标准

1. GitHub 仓库中的常见图片资源可正常显示。
2. GitLab 文档图片渲染能力不受影响。
3. 异常场景下有清晰降级表现。

### 4.7 文档库渲染支持表格内 <br> 换行

#### 需求说明

1. 表格单元格中的 `<br>` 应按预期换行显示。
2. 不影响现有 Markdown 其他内容渲染。

#### 验收标准

1. 表格中的 `<br>` 可正确换行展示。
2. 其他 Markdown 渲染无明显回归。

## UI优化类：

### 4.8 Product 文档标题锚点与 hover 悬浮目录

#### 需求说明

1. Product Markdown 文档当前已支持标题锚点，本期在现有锚点能力基础上补充正文内章节导航体验。
2. 正文卡片右侧内边缘展示一列横杠，作为当前文档章节提示。
3. 每个横杠对应一个标题节点，当前阅读章节对应横杠高亮，其余横杠置灰。
4. 鼠标移入横杠区域后，展开当前文档的悬浮目录。
5. 点击目录项后，正文滚动到对应标题位置，并同步更新 URL `hash`。
6. 点击横杠后，正文滚动到对应标题位置，并同步更新 URL `hash`。
7. 刷新页面后，如 URL 带 `hash`，页面仍应回到对应标题位置。
8. 用户正常浏览、滚动文档时，页面 URL 不自动变化。

#### 方案边界

1. 第一阶段仅支持 Markdown 文档。
2. 目录默认展示 `H1-H3` 标题层级。
3. 标题少于 2 个时，不展示横杠和悬浮目录。
4. 横杠仅支持 hover 展开目录，不支持点击固定展开。
5. 横杠默认态不能明显遮挡正文；悬浮目录展开后不侵占左侧文件树区域。
6. 标题锚点跳转需要避开顶部 Header 遮挡。
7. 横杠最小可点击高度按 `10px` 控制。
8. 页面滚动过程中不自动改写 URL；仅在点击悬浮目录项或横杠时更新 URL。

#### 验收标准

1. Markdown 文档存在 2 个及以上标题时，正文右侧可看到横杠提示。
2. 当前阅读章节对应横杠及悬浮目录高亮。
3. 鼠标移入横杠区域后，可展开目录；默认不支持点击固定展开。
4. 点击目录项后，正文准确滚动到目标标题，且标题不被 Header 遮挡，同时 URL `hash` 正确更新。
5. 点击横杠后，正文准确滚动到目标标题，且 URL `hash` 正确更新。
6. 用户滚动正文时，横杠和目录高亮可同步变化，但页面 URL 不自动变化。
7. 打开带 `#hash` 的链接时，可直接定位到对应标题。
8. 刷新页面后，`#hash` 定位不丢失。
9. 非 Markdown 或无标题文档场景下，不展示错误横杠或错误目录占位。