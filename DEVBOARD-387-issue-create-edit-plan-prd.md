# DevBoard Issue 新建 / 编辑页与关联 Plan 优化 PRD


## 背景

当前 Issue 新建 / 编辑页和详情页在表单承载、关联 Issue、关联 Plan 的交互上不一致，用户经常需要在不同页面之间补操作。

- 新建 / 编辑页需要同时处理基础字段、正文、关联 Issue、关联 Plan。
- 详情页可以处理部分关联关系，但新建 / 编辑页的能力不完整。
- Plan 的取消和切换在不同页面表现不一致。
- 关联关系如果平铺展开，页面会显得过重。


## 目标

- 让 Issue 新建 / 编辑入口统一收敛为右侧抽屉。
- 让基础字段、正文、关联 Issue、关联 Plan 在同一抽屉内顺序清晰、滚动可填。
- 让关联 Issue 和关联 Plan 都支持新增、删除、切换。
- 保持详情页整体结构不变，只收敛编辑动作。

## 非目标

- 不调整 Issue 详情页的主内容布局和信息架构。
- 不把关联关系拆成二级独立页面。
- 不重构 Plan 页面本身。
- 不引入新的复杂审批流或权限模型。
- 不改动 Issue 的核心状态流转规则。
- 不做跨模块的大规模视觉重构。

## 核心方案

### 入口与承载

- 新建 Issue 采用右侧抽屉承载。
- 编辑 Issue 也采用右侧抽屉承载。
- 在详情页中点击“编辑 Issue”后，打开对应 Issue 的编辑抽屉，不在页面中静态铺开编辑区。
- 抽屉宽度保持较大可用区间，正文区域支持独立滚动，底部操作区固定。


### 关联 Issue 区域

- 关联 Issue 区域放在正文下方。
- 关联关系不平铺成一长串按钮，而是通过两个下拉组件完成选择：
  1. 关联关系
  2. 关联 Issue
- 关联关系下拉用于选择关系类型。
- 关联 Issue 下拉用于搜索和选择目标 Issue。
- 选择后才显示“当前待添加”区域，添加完成后自动收起。
- 支持补充信息输入，避免只记录裸关联。
- 每个关联 Issue badge 在悬浮时显示叉号，可直接删除。
- 删除只删除关系本身，不删除 Issue。

### 关联 Plan 区域

- 关联 Plan 使用下拉框完成选择。
- 新建页可选择 Plan，选择后自动落到当前编辑状态。
- 编辑页可对已有 Plan 做取消或切换。
- 已关联 Plan 以白框态展示，悬浮显示叉号。
- hover 的叉号必须放在输入白框内部，不遮挡下拉箭头。
- 点击叉号可取消，点击下拉可切换。

### 详情页保持策略

- Issue 详情页保持和当前真实环境一致的阅读态。
- 详情页只保留“编辑 Issue”作为进入抽屉的入口。
- 详情页中的关联 Plan 卡片保留原有风格。
- 点击“切换”后展开 Plan 列表，而不是固定卡片。

## 规则与门禁

- 右侧抽屉内所有主要输入项都必须可滚动查看，不能被固定页高截断。
- 关联关系支持删除，且删除入口只在 hover / focus 时显现。
- 关联关系选择只保留两个层级的选择入口，不把所有关系类型平铺在页面上。
- 关联关系选择前不展开“当前待添加”区域。
- Plan 列表必须可搜索、可滚动、可选中。
- 下拉内容和真实 DevBoard 的 Plan 列表保持一致风格。
- 切换后立即刷新当前页面显示。
- 取消后恢复为空态或待选态。
- 不破坏现有详情页关联 Plan 的读写能力。
- 不影响新建 / 编辑页其他字段填写和滚动。
- 本期的编辑入口必须指向具体 Issue，不允许做成一个独立的静态编辑页。

## 验收标准

### 新建 / 编辑页

- 用户可以在右侧抽屉内完成产品、issue-key、负责人、正文、关联 Issue、关联 Plan 填写。
- 关联关系通过“关联关系 / 关联 Issue”两个下拉完成选择。
- 关联 Issue badge 悬浮时出现叉号，点击后可移除该关联关系。
- 能在抽屉内完成 Plan 的新增、取消和切换。
- hover 叉号显示在白框内，不与箭头重叠。

### 详情页

- 点击“编辑 Issue”后进入右侧抽屉，不在页面中静态铺开编辑区。
- 点击“切换”后展开 Plan 列表。
- 可以从列表里切换到其他 Plan。
- 取消和切换都能正常回写当前展示。